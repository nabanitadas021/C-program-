#include <stdio.h>
#include <math.h>
int main()
{
	float num,sr;
	printf ("enter the number");
	scanf ("%f", &num);
	sr= sqrt(num);
	printf ("the squareroot of the number is :%.2f\n",sr);
	return 0;
}
