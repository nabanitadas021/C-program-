#include <stdio.h>
int main()
{
	int i,n=0;
	printf("Enter the number:");
	scanf("%d",&n);
	printf("multiplication table:\n");
	for (i=1;i<=10;i++)
	{
	 printf ("%dx%d=%d\n",n,i,n*i);
    }
	return 0;
}
